{
  "name": "application-password-scoper",
  "private": true,
  "devDependencies": {
    "@playwright/test": "^1.54.2"
  },
  "scripts": {
    "playwright:install": "playwright install chromium",
    "test:browser": "playwright test"
  }
}
